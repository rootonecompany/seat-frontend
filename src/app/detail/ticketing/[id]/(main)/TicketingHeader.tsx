import Wrap from "@/components/template/Wrap";
import TicketingThumbanil from "./component/TicketingThumbnail";

export default function TicketingHeader() {
    return (
        <Wrap height="normal" fullWidth={true} as="section">
            <TicketingThumbanil />
        </Wrap>
    );
}
